# =============================================================================
# .dockerignore — não enviar pro build context
# =============================================================================

# build artifacts
dist-newstyle/
dist/
.stack-work/
.cabal-sandbox/
cabal.sandbox.config

# git
.git/
.gitignore
.github/

# editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# logs / temp
*.log
*.tmp
/tmp/

# docs — README.md PRECISA ir pro build context, pois é embutido no
# binário Haskell via Template Haskell (Presentation.Docs) para servir
# o endpoint /docs. DOCS.md e CHANGELOG.md ficam de fora pra reduzir
# o tamanho da imagem (são pesados).
DOCS.md
CHANGELOG.md
# README.md  -- mantido no build context

# front (servido pelo container nginx, não vai pro backend)
front/

# scripts locais
zelaai.sh
seed-demo.sh
smoke-test.sh

# env files
.env
.env.*
